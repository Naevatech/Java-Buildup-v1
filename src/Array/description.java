package Array;

public class description {

    // array is use to group and organize the data
    // array must be declared before it can be use
    //when initial value is not know
    int [] height = new int[10];
    // 1. type of data
    // 2. fact that its an array []
    // 3. number of element to be stored

    //Fortran, matlab array index start from 1
}
